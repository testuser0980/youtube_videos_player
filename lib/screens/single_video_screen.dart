
import 'package:flutter/material.dart';

class SingleVideoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Single Video Screen (replace with detailed UI)')),
    );
  }
}
